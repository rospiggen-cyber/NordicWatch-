const test=require('node:test'),assert=require('node:assert/strict'),R=require('../news-routing'),record=require('./fixtures/masurian-patrol-real.json');
test('real DVIDS Masurian Patrol shape matches its named northeastern Polish node',()=>{const d=R.diagnose(record);assert.equal(record.description,undefined);assert.equal(typeof record.summary,'string');assert.equal(record.entities,undefined);assert(d.normalizedText.includes(record.summary.toLowerCase()));assert(d.extractedCountries.includes('poland'));assert(d.extractedPlaces.includes('bemowo piskie'));assert(d.matchedAliases.includes('masurian patrol'));assert.equal(d.result,'PASS');assert.equal(d.relevanceScore,70);assert.equal(d.inputTypes.location,'undefined')});
test('real record reaches routing at original capture time, without changing freshness today',()=>{const then=R.route([record],{now:Date.parse('2026-09-07T19:37:14.803Z')});assert.equal(then.articles.length,1);const today=R.route([record],{now:Date.parse('2026-09-08T07:42:14.060Z')});assert.equal(today.articles.length,0);assert.equal(today.diagnostics[0].aoiRelevant,true);assert.match(today.diagnostics[0].exclusionReason,/window/i)});
